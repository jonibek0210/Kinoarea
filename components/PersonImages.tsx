/* eslint-disable @next/next/no-img-element */
const PersonImages: React.FC<any> = (props) => {
   const { images } = props
   console.log(images);

   return (
      <div className="w-full">
         
      </div>
   )
}

export default PersonImages